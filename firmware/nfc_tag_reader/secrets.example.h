// Copy this file to secrets.h (same folder) and fill in your values.
// secrets.h is gitignored so real credentials never land in the repo.
//
// The starter sketch does not use these yet; they're here so the WiFi
// step has a home once it's added.

#pragma once

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// URL of the backend that maps tag UID -> Spotify play command.
#define BACKEND_URL   "https://your-backend.example.com/play"
